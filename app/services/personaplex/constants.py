# NVIDIA PersonaPlex model served on Modal (the HF repo loaded in inference/voice_app.py). Stored per session so cost/usage analysis can tell PersonaPlex sessions apart from OpenAI ones.
PERSONAPLEX_MODEL = "nvidia/personaplex-7b-v1"
